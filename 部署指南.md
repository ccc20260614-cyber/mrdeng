---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: d92f12c577d2fb5dc47f8472c4a16c08_6e4597ef871d11f18766525400f8a581
    ReservedCode1: IGLPEG4Wq29ie1ofU9f9zIL5J3eIdU/eEQ5Ik5XJx2ByMbGwJRSRGh3Efyo+GR91CrJoPDC4rCIJapTsqkR6iThjo6rOGbxfmNzj3msG0R7v5NPK0F1l1z+tfCXocqmp/6QSVJ663GnEnNxrBRrdWpeeb7S+S2pI/lTftbRkyFMzztakEuEzVLsoUMg=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: d92f12c577d2fb5dc47f8472c4a16c08_6e4597ef871d11f18766525400f8a581
    ReservedCode2: IGLPEG4Wq29ie1ofU9f9zIL5J3eIdU/eEQ5Ik5XJx2ByMbGwJRSRGh3Efyo+GR91CrJoPDC4rCIJapTsqkR6iThjo6rOGbxfmNzj3msG0R7v5NPK0F1l1z+tfCXocqmp/6QSVJ663GnEnNxrBRrdWpeeb7S+S2pI/lTftbRkyFMzztakEuEzVLsoUMg=
---

# MrDeng 设备管理面板 - 部署指南

## 系统要求

| 项目 | 最低要求 |
|------|----------|
| 操作系统 | CentOS 7+ / RHEL 8+ / Debian 10+ / Ubuntu 20.04+ |
| 内存 | 512 MB |
| 磁盘 | 1 GB 可用空间 |
| Python | 3.8+ |
| 端口 | 80（HTTP）/ 443（HTTPS，可选） |

## 快速安装

### 1. 上传部署包

将 `mrdeng_v1.0.tar.gz` 上传到服务器，解压：

```bash
tar -xzf mrdeng_v1.0.tar.gz
cd mrdeng_v1.0
```

### 2. 运行安装脚本

```bash
sudo bash install.sh
```

安装脚本会自动完成以下步骤：
- 安装 Python3、pip、Nginx
- 复制程序文件到 `/opt/mrdeng`
- 安装 Python 依赖（Flask、Gunicorn 等）
- 初始化 SQLite 数据库
- 创建 systemd 服务并启动
- 配置 Nginx 反向代理
- 放行防火墙端口

安装过程中会询问域名（直接回车可跳过）。

### 3. 访问面板

安装完成后，浏览器访问服务器 IP 或域名，使用默认密码登录：

- 地址: `http://你的服务器IP`
- 密码: **admin123**

## 自定义安装

### 指定安装目录

```bash
sudo bash install.sh /data/mrdeng
```

### 修改管理员密码

编辑 `/etc/systemd/system/mrdeng.service`，修改 `MRDENG_PASS` 环境变量，然后重启：

```bash
sudo systemctl daemon-reload
sudo systemctl restart mrdeng
```

### 修改数据库路径

同上，修改 `MRDENG_DB` 环境变量即可。

## 服务管理

```bash
# 启动
sudo systemctl start mrdeng

# 停止
sudo systemctl stop mrdeng

# 重启
sudo systemctl restart mrdeng

# 查看状态
sudo systemctl status mrdeng

# 查看实时日志
sudo journalctl -u mrdeng -f

# 查看 Nginx 日志
tail -f /opt/mrdeng/logs/nginx_access.log
```

## 配置 HTTPS（可选）

### 使用 Let's Encrypt 免费证书

```bash
# 安装 certbot（CentOS）
sudo yum install -y certbot python3-certbot-nginx

# 安装 certbot（Debian/Ubuntu）
sudo apt-get install -y certbot python3-certbot-nginx

# 申请证书（请先确保域名 DNS 已解析到服务器）
sudo certbot --nginx -d 你的域名

# 自动续期
sudo certbot renew --dry-run
```

## 使用入门

### 登录后操作流程

1. **生成卡密** - 点击「卡密管理」，选择等级（基础版/专业版/旗舰版/至尊版），填写描述和有效期
2. **注册设备** - 点击「添加设备」，输入设备名称，快速注册；或将卡密分发给客户自行绑定
3. **分发任务** - 在「任务面板」创建新任务，指定任务类型和目标 URL
4. **监控状态** - 「总览」页面查看在线设备数、待处理任务数

### 卡密等级说明

| 等级 | 前缀 | 设备上限 | 有效期 |
|------|------|----------|--------|
| 基础版 | BASIC | 3 台 | 30 天 |
| 专业版 | STUDIO | 10 台 | 180 天 |
| 旗舰版 | PRO | 50 台 | 365 天 |
| 至尊版 | ULT | 无限制 | 永久 |

### 默认卡密

安装后系统自动包含一张默认卡密：`STUDIO-PRO-8888`（专业版，10 设备，180 天）。可在卡密管理中删除。

## 故障排查

| 问题 | 解决方法 |
|------|----------|
| 无法访问 | 检查防火墙：`sudo firewall-cmd --list-all`，确认 80 端口开放 |
| 502 错误 | Gunicorn 未启动：`sudo systemctl status mrdeng`，查看日志 |
| 页面加载异常 | 清除浏览器缓存后刷新；检查 Nginx 日志 |
| 数据库错误 | 确认 `mrdeng.db` 文件存在且可写：`ls -la /opt/mrdeng/mrdeng.db` |

## 文件结构

```
/opt/mrdeng/
├── app.py              # Flask 主程序
├── harvester.py        # 任务收割器（定时注入外网任务）
├── requirements.txt    # Python 依赖
├── mrdeng.db           # SQLite 数据库
├── logs/               # 运行日志
│   ├── gunicorn.log
│   ├── nginx_access.log
│   └── nginx_error.log
└── templates/
    ├── index.html      # 管理面板页面
    └── login.html      # 登录页面
```
*（内容由AI生成，仅供参考）*
